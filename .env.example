# ==== DEPLOY NO RENDER ====
# O Render configura automaticamente DATABASE_URL quando você adiciona um banco PostgreSQL
# Você só precisa definir DB_TYPE=postgres no painel do Render

# Tipo de banco de dados: 'mysql' ou 'postgres'
DB_TYPE=postgres

# ==== Para PostgreSQL (Render automático) ====
# O Render preenche DATABASE_URL automaticamente - não precisa definir manualmente
# DATABASE_URL=postgresql://usuario:senha@host:porta/banco

# ==== Para MySQL (configuração manual) ====
# Se usar MySQL externo, defina DB_TYPE=mysql e configure:
# DB_HOST=seu_host_mysql.com
# DB_PORT=3306
# DB_USER=seu_usuario
# DB_PASSWORD=sua_senha
# DB_NAME=bragawork_db

# ==== Configurações do servidor ====
PORT=5000
NODE_ENV=production
